#include <stdio.h>
int sumn(int n);

int main()
{
    printf(" sum is : %d" , sumn(36));
   
    return 0;
}
int sumn(int n){
    if(n==1){
        return 1;
    }

     int sumnm1=sumn(n-1);
     int sumn=sumnm1+n;
    return sumn;
}
