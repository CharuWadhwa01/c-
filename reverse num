#include<stdio.h>
void main()
{
    int n,rev=0,num;
    printf("enter a number");
    scanf("%d",&n);
    while (n!=0)
    {
        num=n%10;
        rev=rev*10+num;
        n=n/10;
    }
    printf("reverse is=%d",rev);
    
}
