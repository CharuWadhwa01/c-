#include<stdio.h>
int main(int argc, char const *argv[])
{
    int age=22;
    int *ptr=&age;
    int _age=25;
    int *_ptr=&_age;
    printf(" %u ,%udiffrence is:%u\n",ptr,_ptr ,ptr-_ptr);
    _ptr=&age;
    printf("comparison is:%u\n",ptr==_ptr);

    return 0;
}
