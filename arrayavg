#include<stdio.h>
void main()
{
    int a[5],i,sum=0;
    float avg[i];
    for ( i = 0; i < 5; i++)
    {
        scanf("%d",&a[i]);
    }
    for ( i = 0; i < 5; i++)
    {
        sum=sum+a[i];
        avg[i]=sum/5.0;
    }
        printf("%f",avg[i]);
    
    
}
