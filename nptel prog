
#include<stdio.h>
#define PI 3.14

float myfun(float radius)
{
    float a;
    a = PI * radius * radius;
    return a;
}

int main()
{
    float radius, area;
    scanf("%f", &radius);
    area = myfun(radius);
    printf("%f", area);
    return 0;
}
